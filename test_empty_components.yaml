# this configuration file has all empty custom components set up.
# use 'esphome test_empty.yaml compile' to build. 

packages:

printer_component:
  id: printer_component_1

uart:
  tx_pin: D0
  rx_pin: D1
  baud_rate: 9600
